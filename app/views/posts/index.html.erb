<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.title, post_path(post) %>
      <p><%= post.callout %></p>
    </li>
  <% end %>
</ul>

<%= link_to "New Post", new_post_path %>
